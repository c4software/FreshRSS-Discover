Manage your feeds from the app.

• In Settings → Manage feeds: the account's feeds, as FreshRSS holds them.
• Add a feed by its address; the app checks it before sending and says what the server refused.
• Remove a feed with a tap on the bin, after confirmation.
• Nothing gets organised here — categories, renaming and sorting stay in FreshRSS.
