Immersive mode reloads like the list.

• No more automatic reload when coming back to the app: list and immersive mode now show the same articles, in the same order.
• Re-tapping the tab returns to the first page, without reloading when already there.
• A veil covers the feed during every requested reload.
